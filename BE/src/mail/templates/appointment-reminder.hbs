<html>
  <head>
    <meta charset='utf-8' />
    <title>Appointment Reminder</title>
    <!-- 
    Note: Some email clients ignore external CSS files. 
    Inline or embedded styles (like below) are often safer. 
  -->
    <style>
      body { margin: 0; padding: 0; background-color: #f2f2f2; font-family:
      Arial, sans-serif; } .email-container { max-width: 600px; margin: 30px
      auto; background-color: #ffffff; border-radius: 8px; overflow: hidden; }
      .header { background-color: #4c68d7; /* Customize the header color */
      padding: 20px; text-align: center; color: #ffffff; } .header h1 { margin:
      0; font-size: 24px; font-weight: 600; } .content { padding: 20px; }
      .content p { margin: 16px 0; line-height: 1.5; color: #333333; } .info-box
      { background-color: #f7f7f7; border-radius: 4px; padding: 20px; margin:
      20px 0; } .info-box strong { display: inline-block; margin-bottom: 8px;
      color: #555555; } .avatar-container { text-align: center; margin: 20px 0;
      } .avatar { width: 80px; height: 80px; border-radius: 50%; object-fit:
      cover; } .footer { text-align: center; padding: 20px; color: #888888;
      font-size: 14px; }
    </style>
  </head>
  <body>
    <div class='email-container'>
      <!-- Header Section -->
      <div class='header'>
        <h1>Appointment Reminder</h1>
      </div>

      <!-- Main Content Section -->
      <div class='content'>
        <p>Hello {{patientName}},</p>
        <p>
          This is a friendly reminder for your upcoming appointment with Dr.
          <strong>{{doctorName}}</strong>.
        </p>

        <!-- Appointment Details Box -->
        <div class='info-box'>
          <strong>Date & Time:</strong>
          <p>
            <strong>From:</strong>
            {{appointmentStartTime}}
            <br />
            <strong>To:</strong>
            {{appointmentEndTime}}
          </p>

          {{#if notes}}
            <strong>Notes:</strong>
            <p>{{notes}}</p>
          {{/if}}
        </div>

        <p>
          If you have any questions or need to reschedule, please contact Dr.
          {{doctorName}}
          at
          <a href='mailto:{{doctorEmail}}'>{{doctorEmail}}</a>.
        </p>

        <!-- Doctor Avatar -->
        <div class='avatar-container'>
          <img src='{{doctorAvatar}}' alt='Doctor Avatar' class='avatar' />
        </div>

        <p>Thank you for choosing our services.</p>
        <p>
          Best regards,<br />
          Your Healthcare Team
        </p>
      </div>

      <!-- Footer Section -->
      <div class='footer'>
        &copy;
        {{year}}
        DermaAI Skinning Cancer Prediction Platform
      </div>
    </div>
  </body>
</html>